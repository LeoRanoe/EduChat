# 🚀 Deploy EduChat to Render - Step by Step

## ✅ Your Code is Ready!
- Branch: **experiment**
- Latest commit pushed ✅
- render.yaml configured ✅

---

## 📋 Deployment Steps

### Step 1: Go to Render Dashboard
1. Open: https://dashboard.render.com/
2. Sign in with GitHub (or create account if needed)

### Step 2: Create New Web Service
1. Click **"New +"** button (top right)
2. Select **"Web Service"**
3. Click **"Connect a repository"** → Find **EduChat**
4. Click **"Connect"**

### Step 3: Configure Service (Auto-filled from render.yaml)
Render will automatically detect your `render.yaml` file and configure:

✅ **Name:** educhat  
✅ **Environment:** Python  
✅ **Branch:** experiment  
✅ **Build Command:** Auto-configured  
✅ **Start Command:** Auto-configured  

Just click **"Next"** or **"Apply"**

### Step 4: Add Environment Variables (REQUIRED!)

Click on **"Environment"** tab and add these variables:

#### 🔑 Required Variables:

```
GOOGLE_AI_API_KEY
Your Google Gemini API key
Get from: https://aistudio.google.com/app/apikey

APP_ENV
production

PYTHON_VERSION
3.11
```

#### 📊 Optional Variables (if using Supabase):

```
DATABASE_URL
Your Supabase connection string

SUPABASE_URL
Your Supabase project URL

SUPABASE_ANON_KEY
Your Supabase anonymous key

SUPABASE_SERVICE_ROLE_KEY
Your Supabase service role key
```

**Note:** `SESSION_SECRET` will be auto-generated by Render ✅

### Step 5: Deploy!
1. Click **"Create Web Service"** button
2. Wait 5-10 minutes for build and deployment
3. Watch the logs for any errors

### Step 6: Get Your Live URL
Once deployed, Render will give you a URL like:
```
https://educhat-xxxx.onrender.com
```

---

## 🎯 Quick Deploy Checklist

- [ ] Signed into Render dashboard
- [ ] Connected GitHub repository (EduChat)
- [ ] Selected "experiment" branch
- [ ] Added GOOGLE_AI_API_KEY environment variable
- [ ] Added APP_ENV=production
- [ ] Clicked "Create Web Service"
- [ ] Waited for deployment to complete
- [ ] Tested the live URL

---

## 🐛 Common Issues & Fixes

### Build Fails?
- Check that `requirements.txt` is up to date
- Verify Python version is 3.11 in environment variables
- Check build logs for missing dependencies

### App Crashes on Start?
- Verify GOOGLE_AI_API_KEY is set correctly
- Check that APP_ENV=production
- Review runtime logs in Render dashboard

### AI Not Responding?
- Verify your Gemini API key is valid
- Check quota hasn't been exceeded
- Test with: https://aistudio.google.com/app/apikey

---

## 🔄 Update Deployment

To deploy new changes:

```bash
# Make your changes
git add .
git commit -m "Your changes"
git push origin experiment
```

Render will auto-deploy! ✨ (if auto-deploy is enabled)

---

## 📞 Need Help?

- Render Docs: https://render.com/docs
- Render Community: https://community.render.com/
- Check deployment logs in Render dashboard

---

## 🎉 That's It!

Your EduChat app will be live at your Render URL with:
- ✅ Google Gemini AI integration
- ✅ Beautiful ChatGPT-like UI
- ✅ Smooth animations
- ✅ Auto-scroll
- ✅ Enter key to send
- ✅ Responsive design

Enjoy your deployed app! 🚀
