# =============================================================================
# EduChat Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!
# 
# For Render deployment: Set these in Render Dashboard > Environment
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from: Supabase Dashboard > Project Settings > API
# 
# Direct connection URL (for migrations/admin tasks)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Supabase REST API endpoint
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co

# Public anon key (safe for frontend)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key (keep secret! backend only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -----------------------------------------------------------------------------
# AI Service Configuration (REQUIRED for chatbot)
# -----------------------------------------------------------------------------
# Choose ONE of the following:

# Option 1: OpenAI (GPT-3.5/GPT-4)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here

# Option 2: Google AI (Gemini)
# Get from: https://makersuite.google.com/app/apikey
# GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Environment: development, staging, or production
APP_ENV=development

# Enable debug mode (set to False in production)
DEBUG=True

# -----------------------------------------------------------------------------
# Session & Security Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Generate a secure random key:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your_random_secret_key_here

# -----------------------------------------------------------------------------
# Optional: Additional Configuration
# -----------------------------------------------------------------------------
# App name (for branding)
APP_NAME=EduChat

# App version
APP_VERSION=1.0.0

# Log level: debug, info, warning, error
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Render Deployment Notes
# -----------------------------------------------------------------------------
# When deploying to Render:
# 1. Go to Render Dashboard > Your Service > Environment
# 2. Add each variable above (except comments)
# 3. Use "Generate Value" for SESSION_SECRET
# 4. Set APP_ENV=production and DEBUG=False
# 5. Keep SUPABASE_SERVICE_ROLE_KEY secure (never log or expose)
# =============================================================================
